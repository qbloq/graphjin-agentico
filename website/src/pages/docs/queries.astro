---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="Queries">
  <h1>Queries</h1>
  
  <p>GraphJin supports a rich query syntax for fetching data, including nested relations, aggregations, and complex filtering.</p>

  <h2>Basic Select</h2>
  <pre><code>{`query {
  users {
    id
    email
  }
}`}</code></pre>

  <h2>Nested Select</h2>
  <pre><code>{`query {
  users {
    id
    email
    posts {
      title
      comments {
        body
      }
    }
  }
}`}</code></pre>

</DocsLayout>
